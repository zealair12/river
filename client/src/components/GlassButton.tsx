export function GlassButton({ children, variant='primary', onClick }:{children:React.ReactNode;variant?:'primary'|'ghost'|'danger';onClick?:()=>void}) { const glow=variant==='danger'?'rgba(255,77,106,.4)':variant==='primary'?'rgba(0,229,200,.35)':'transparent'; return <button onClick={onClick} style={{background:variant==='ghost'?'transparent':'var(--glass-bg)',border:'1px solid var(--glass-border)',color:'white',padding:'8px 12px',borderRadius:10,boxShadow:`0 0 16px ${glow}`}}>{children}</button>; }
